# Upstash Redis (serverless) — powers the live presence badge + global view counts/trending.
# Get these from the Upstash console (REST API section). Without them, these features degrade
# gracefully (in dev an in-memory fallback is used; in prod the affected UI stays hidden).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
