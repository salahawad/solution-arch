node_modules/
dist/
.output/
output/
.astro/
.vercel/
*.log
.env
.env.*
!.env.example
.DS_Store

# Local-only: Claude config and superpowers specs (kept on disk, not on GitHub)
.claude/
docs/superpowers/

# Intermediate Motion Canvas build dir (per-concept). The publishable runtime assets
# (bundles, core.js, player.js, posters, manifest) are committed under
# packages/site/public/animations + packages/site/src/concepts.json so Vercel only
# needs to run `astro build`. Regenerate them with `pnpm rebuild:all`.
packages/animations/dist/

# Author-only reel artifacts (rendered MP4s + captions) — never deployed, just posted
packages/animations/reels/

# Local scratch: downloaded reference reels and other throwaway files
temp/
